{
  "name": "postman",
  "version": "1.0.0",
  "description": "Project for storing Postman tests",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "api-test": "newman run tests/Postman_Jenkins.postman_collection.json --reporters cli,junit --reporter-junit-export newman.xml --insecure"
  },
  "author": "Mary0707",
  "dependencies": {
    "newman": "^4.4.1"
  }
}
